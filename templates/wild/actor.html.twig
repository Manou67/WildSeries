{% extends 'base.html.twig' %}

{% block body %}
<h2>{{ actor.name }}</h2>
    {% for program in actor.programs %}
        <a href="{{ path('wild_show',{"slug" : program.slug | lower | replace({" " : "-"})}) }}">{{ program.title }}</a>
        <br/>
    {% endfor %}
{% endblock %}
